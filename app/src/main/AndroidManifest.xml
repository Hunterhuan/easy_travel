<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrhan.maketravel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Spots">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Selectcity" />
        <activity android:name=".Login" />
        <activity android:name=".SelectDate" />
        <activity android:name=".travel_tab" />
        <activity android:name=".spots_detail" />
        <activity android:name=".result" />
        <activity android:name=".Registe" />
        <activity android:name=".person_page" />
        <activity android:name=".Edit_person_info"></activity>
    </application>

</manifest>